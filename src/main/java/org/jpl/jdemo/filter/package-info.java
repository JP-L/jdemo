/**
 * Intercepting Filters as pluggable filters to pre and postprocess requests 
 * and responses. A filter manager combines loosely coupled filters in a chain,
 * delegating control to the appropriate filter. In this way, you can add, 
 * remove, and combine these filters in various ways without changing existing
 * code
 * <p>
 *
 * @since 1.0
 * @author limpens
 */
package org.jpl.jdemo.filter;